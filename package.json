{
  "name": "edge-notes",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev ./public --filesystem-routing --local --project-name edge-notes",
    "build": "tailwindcss -i ./public/styles.css -o ./public/styles.generated.css",
    "preview": "wrangler pages dev ./public --project-name edge-notes",
    "deploy": "wrangler pages deploy ./public --project-name edge-notes"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251211.0",
    "tailwindcss": "^3.4.14",
    "wrangler": "^4.54.0"
  }
}
